package network_data.compact_flow;

import network_data.FlowLike;

/* A compact flow is a list of fields or a Void flow
 * (i.e. impossible - used for error cases or constraint violation cases).
 */
public abstract class CompactFlow implements FlowLike<CompactFlow> {
}
